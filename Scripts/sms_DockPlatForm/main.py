import time

from msg.taxin import taxin
import re



if __name__ == '__main__':
    # taxin = taxin()
    # info = taxin.Login('iavein', 'zdy20000519')
    # taxin.GetPhone('hh','222',type='json')

    # data = '您的码是：25687'
    # code = str(re.findall(r'：.*', data)[0])[1:]
    # print(code)
